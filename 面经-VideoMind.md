# VideoMind 后端开发岗位面经

## 1. 项目简介

我设计并实现了一套面向长视频的多模态内容理解平台：通过可靠的大文件上传与异步任务框架完成音频转写、关键帧识别、时序融合和知识索引，再结合关键词与向量混合检索、证据审查工作流提供可追溯问答。系统重点解决了消息与任务一致性、重复消费、阶段恢复、跨存储补偿、检索降级和智能体调用边界等后端工程问题。

## 2. 简历表述

- 基于 RocketMQ 事务消息将视频解析迁移到后台执行，在同一本地事务内落库任务、事件及业务关联，并以消费 Inbox、乐观锁租约和阶段检查点收敛至少一次投递带来的重复执行。
- 设计分片上传与断点续传链路，使用 Redis Bitmap 压缩记录分片状态、Redisson 细粒度锁保证分片幂等，并以用户维度 MD5 唯一约束解决并发去重竞态。
- 将 ASR 与关键帧 OCR 并行编排，采用重叠音频窗口和中点归属规则消除边界重复，按时间戳、文本相似度及置信度融合为可检索的多模态时间轴。
- 基于 Elasticsearch 构建带租户和知识库过滤的混合检索，组合 BM25、向量召回、RRF 排名融合与 Rerank 精排，并为单路依赖故障提供降级路径。
- 实现有界的 Planner–Executor–Critic 问答工作流，以工具白名单、调用预算、截止时间、证据审查和受约束查询改写控制无关检索与无依据引用。
- 建立覆盖消息重投、Broker 重启、Redis 故障、长音频、删除和问答链路的自动化验证基线；性能、检索质量与故障恢复量化指标按压测方案持续补充，不使用未经测量的数据。

## 3. 高频面试题与追问

### 主题一：异步任务与一致性

#### Q1：请你介绍一下这个项目的后端整体架构，为什么要把视频解析做成异步任务？

**回答：** 场景上，我面对的是从大文件上传到音频提取、语音识别、画面文字识别、摘要和索引的一条长链路，单次处理可能远超 HTTP 请求可承受时间。根因是媒体计算和外部模型调用都具有高延迟、易超时、资源消耗不均的特点，如果同步串联会占满 Web 线程，也无法在进程重启后恢复。行动上，我把接入、任务控制、媒体处理、知识索引和问答拆成边界清晰的模块，以消息触发后台任务，用持久化状态机记录进度，用检查点保存阶段产物，并让 ASR 与 OCR 并行。结果是请求可快速返回任务标识，后台能够重试和续跑；即使某个外部能力失败，也能按阶段语义降级或进入待重试状态，而不是让整条请求静默丢失。

**追问 1：为什么不直接使用线程池异步执行？**

**回答：** 场景上，我最初需要的并不只是“让接口快点返回”，而是让任务跨进程、跨重启仍然可追踪。根因是本地线程池的队列和执行上下文主要存在于进程内：应用发布或崩溃时，未执行任务可能丢失；多实例之间也缺少统一重试和削峰机制。行动上，我保留有界线程池用于一次请求内部的小粒度并行和超时控制，但把业务级长任务交给消息系统，并在数据库持久化任务、事件、尝试次数和检查点。结果是消息系统负责分发与重投，数据库负责事实状态，线程池只承担局部计算；如果消息设施暂不可用，任务仍可通过事件记录核查，而不是依赖 JVM 内存恢复。

**追问 2：这条链路里哪些地方是同步的，哪些地方是异步的？**

**回答：** 场景上，我需要同时保证用户立即获得反馈，以及后台任务确实具备可执行依据。根因是如果所有步骤都异步，上传完整性和任务受理结果无法及时确认；如果全部同步，媒体解析会拖垮接口。行动上，我把鉴权、分片校验、合并校验、文件元数据写入和任务受理放在请求边界内同步完成，接口随后返回文件或任务标识；消息消费后异步执行音频提取、ASR、OCR、时间轴、摘要和索引。单个解析任务内部，ASR 与 OCR 又并行执行，但最终融合和发布必须等待必要产物。结果是同步部分只承担确定性接入，异步部分承载高延迟工作，用户可以查询状态，系统也能在阶段边界恢复。

#### Q2：你如何保证数据库任务创建和 RocketMQ 消息投递的一致性？

**回答：** 场景上，我需要避免数据库已有任务却没有消息，以及重复请求为同一个活动任务产生多条有效消息。根因是 MySQL 与 Broker 不能由普通本地事务原子提交，并发请求又可能同时通过应用层查询。行动上，我先发送半消息，再以活动业务指纹唯一约束裁决本地事务：获胜者创建任务、业务记录和事务事件并返回 COMMIT；并发输家读取获胜任务，不再写新事件，向 Broker 返回 ROLLBACK_MESSAGE 丢弃自己的半消息，但接口仍正常返回原任务和规范事件 ID；真实异常才返回失败。结果是一个活动任务只保留一个可投递事件，Broker 状态不确定时仍可按该事件回查，消费端再按至少一次语义处理同一事件的重投。

**追问 1：事务回查时为什么查询事件表，而不是查询任务状态？**

**回答：** 场景上，Broker 回查要回答的是“这一次本地消息事务是否提交”，而不是“耗时任务是否已经执行完成”。根因是业务任务在消息提交后可能仍处于待处理、处理中甚至重试状态，直接用任务最终状态判断会把合法的未完成任务误判为回滚。行动上，我为每次事务消息建立稳定的事件标识，并把事件与任务、业务关联放在同一本地事务中；回查只读取事件是否已经形成已提交事实，对不存在或仍不确定的记录采取保守策略。结果是回查语义和本地事务边界一致，不受后台处理进度影响；即使回查重复发生，读取同一事件事实也不会产生额外副作用。

**追问 2：如果本地事务执行了两次，会不会创建两个任务？**

**回答：** 场景上，同一业务请求可能被客户端并发提交，两次调用在发送半消息时已经各自生成临时事件 ID。根因是“先查再插”存在竞态，如果两个本地事务都提交消息，就会出现不同事件竞争同一任务。行动上，我用稳定业务指纹和活动指纹唯一索引让数据库选出唯一获胜者：获胜事务创建任务、业务记录和规范事件并提交半消息；输家复用获胜任务及其规范事件 ID，不写自己的事件，并回滚自己的半消息。生产者把这种 ROLLBACK_MESSAGE 识别为正常复用结果，而不是请求失败。结果是重复请求返回同一个任务，同时只有获胜消息进入消费链路；真实数据库异常没有复用结果，仍按失败返回。

#### Q3：消息至少一次投递下，你怎样实现幂等、重试和断点续跑？

**回答：** 场景上，即使一个活动任务只提交一个规范事件，RocketMQ 仍可能按至少一次语义重复投递该事件，消费者也可能在任意阶段崩溃。根因是消费确认与业务副作用不在同一事务，单一“处理过”标志无法覆盖处理中断和旧实例恢复。行动上，我先用消费组与事件 ID 唯一的 Inbox 记录领取和完成；未完成的重复投递继续进入任务状态机，以 owner、租约和版本条件竞争执行权；只有 ACQUIRED 才调用处理器，终态直接补全 Inbox；阶段检查点按任务和阶段唯一保存产物校验信息。结果是已完成事件快速返回，崩溃任务可在租约后接管并从检查点续跑。边界上，当前主链尚未周期续租，需补充短租约看门狗。

**追问 1：Inbox 和业务幂等键是不是重复设计？**

**回答：** 场景上，两者都表现为防重复，但防护对象不同。根因是客户端可以重复提交业务请求，Broker 也可以重复投递同一个已提交事件。行动上，我用业务指纹和活动任务唯一索引选出唯一任务，并让复用请求回滚自己的半消息，因此它解决“是否创建和投递第二份业务”；Inbox 则以消费组和规范事件 ID 唯一记录处理中与已完成，解决“同一已提交事件是否再次执行业务”。处理中记录不能直接视为完成，仍要交给租约状态机判断接管；阶段副作用再由检查点保护。结果是请求、事件、任务执行和阶段重放各自在对应层收敛，Inbox 不被误称为端到端 exactly-once。

**追问 2：消费者处理完成后、确认消息前宕机会怎样？**

**回答：** 场景上，这是至少一次消息最典型的重复窗口：业务可能已经落库或写入索引，但 Broker 没收到成功确认。根因是业务提交和消息确认无法处在同一个本地事务里。行动上，我接受消息会再次投递，重启后先检查 Inbox 和任务状态；若整个事件已标记完成就直接返回，若只完成部分阶段，就校验对应检查点和产物后继续未完成步骤。对索引等可重复写副作用，我使用稳定文档身份和版本化发布，让重放覆盖或复用而非追加脏数据。结果是第二次消费不会盲目从头执行，最终状态能够收敛；若外部系统不支持幂等，则必须额外记录请求标识并做补偿核对。

#### 补充推演：重复投递、并发消费与租约边界

**1. 两个消费者同时收到同一个 `event-A` 会怎样？**

RocketMQ 按至少一次语义重投时，两个消费调用都可能进入。二者首先根据 `event-A` 查询事务事件，只有状态为 `COMMITTED` 才继续；随后同时向 Inbox 插入 `(consumer_group, event_id)`。数据库唯一约束只允许一条 `PROCESSING` 记录，另一方得到 `IN_PROGRESS`。当前实现不会把 `IN_PROGRESS` 当成完成，因为原处理者可能已经中断，而是继续向状态机申请同一个 `task-1001` 的执行权。两个调用分别生成 `group:uuid-A` 和 `group:uuid-B`，再以读取到的 `state_version` 执行条件更新：获胜者在一条 SQL 中写入 owner、租约到期时间并将版本加一；失败者得到 `BUSY`，不进入视频处理器，而是结束本次调用并交由 MQ 的有限重投机制稍后再试。

```sql
UPDATE processing_task
SET state = 'PROCESSING',
    lease_owner = ?,
    lease_expires_at = ?,
    state_version = state_version + 1
WHERE id = 1001
  AND state_version = ?
  AND 当前状态、重试时间与租约条件允许执行;
```

**2. 重复请求还会产生不同 `eventId` 指向同一活动任务吗？**

正常生产链路不会。两个请求虽然会先各自发送半消息，但本地事务通过 `active_fingerprint` 唯一索引选出获胜者：请求 A 创建 `task-1001` 和规范事件 `event-A`，返回 COMMIT；请求 B 插入任务失败后读取并复用 `task-1001` 与 `event-A`，不写 `event-B` 事件，返回 ROLLBACK_MESSAGE 丢弃自己的半消息，同时接口正常返回 `reused=true`。因此消费者只会看到规范事件 `event-A`，不再让一个健康消费者因为冗余 `event-B` 获取租约失败、反复重试并进入死信队列。历史数据、人工重放或异常消息仍需由任务状态机按 `taskId` 兜底，但它们不是正常重复请求的产物。

```text
请求 A：新任务 + COMMIT
→ 创建 task-1001、event-A
→ event-A 可以投递

请求 B：复用任务 + ROLLBACK_MESSAGE
→ 返回 task-1001、event-A、reused=true
→ 临时 event-B 半消息被丢弃

真实本地事务异常
→ 没有可靠任务结果
→ 接口返回失败
```

**3. 能不能说同一个任务一定不会重复执行？**

不能说端到端 exactly-once。正常租约有效期内，同一 `taskId` 只有一个消费者能通过 CAS 获得执行权；重复投递如果发现任务已经终态，只补全 Inbox 而不调用处理器。但当前主处理链尚未周期调用 `renew()`，配置文件中的有效默认租约为 600 秒。健康任务超过租期后，如果同一规范事件因消费超时或异常再次投递，第二消费者可能接管，而第一消费者的外部 ASR、大模型或 MinIO 请求未必能被撤销。版本和 owner 能阻止旧消费者在接管后覆盖最终任务状态，阶段检查点能减少重做；完整修复仍应采用短租约看门狗、续租失败即停止写入，并确保 MQ 重试时间窗口覆盖租约时长。

### 主题二：大文件上传与存储一致性

#### Q4：分片上传和断点续传是怎么设计的？

**回答：** 场景上，视频文件体积大，弱网环境下整文件失败后重传会浪费时间和带宽，同时用户可能并发重复提交某一分片。根因是单次 HTTP 上传缺少细粒度进度，服务端也需要判断分片是否完整且未被篡改。行动上，我先创建带过期时间的上传会话，客户端按编号上传分片；服务端校验分片编号、大小和 MD5，在分片粒度获取分布式锁，再以临时文件加原子移动落盘，并在 Redis Bitmap 中设置完成位。查询进度时返回缺失编号，完成阶段先检查位图、顺序合并并再次计算整文件 MD5。结果是重复分片可幂等返回，网络中断后只补传缺失部分；当前 Bitmap 丢失后还不能从磁盘自动重建，这是我明确保留的改进项。

**追问 1：为什么用 Bitmap，不直接在 MySQL 中每个分片存一行？**

**回答：** 场景上，一个大文件可能包含大量分片，上传过程中进度查询和置位都很频繁。根因是每片一行会放大数据库写入、索引和清理成本，而分片状态本质上主要是连续编号上的布尔值。行动上，我用 Bitmap 将第 N 个分片映射为第 N 位，置位和检查缺失集合都在 Redis 内完成，MySQL 只保存会话和最终文件等需要长期一致性的元数据；分片实体先落本地临时目录。结果是状态空间紧凑、更新开销低，也降低了主库压力。代价是 Redis 状态在当前实现中不只是可丢缓存，因此我会通过持久化配置、合理 TTL，并进一步增加扫描临时目录重建位图的能力来降低风险。

**追问 2：分片锁为什么还不够保证绝对安全？**

**回答：** 场景上，我用分布式锁阻止两个请求同时写同一会话的同一编号，但锁配置采用固定租期。根因是如果极慢网络、磁盘抖动或线程停顿让处理时间超过租期，锁会提前释放，另一个请求可能进入；另外锁服务不可用时也需要明确失败策略。行动上，我在锁内仍执行内容摘要校验、临时文件写入和原子移动，使重复写的最终目标尽量确定，并让完成阶段再次校验整文件摘要。结果是常见重复请求能够安全收敛，但我不会声称锁提供绝对 exactly-once；下一步会使用可续期看门狗或基于令牌的 fencing，并用超时分片压测验证锁过期窗口。

#### Q5：你如何通过文件 MD5 去重，并处理并发上传同一文件？

**回答：** 场景上，同一用户可能重复上传相同内容，两个请求也可能几乎同时完成合并。根因是只在写入前查询 MD5 存在并发竞态，而把 MD5 全局唯一又会错误地把不同用户的数据归为同一权限对象。行动上，我先用用户标识和整文件 MD5 查询并快速复用；真正插入时由数据库上的用户维度活动文件唯一约束裁决。为兼容软删除后重新上传，我使用仅对未删除记录产生值的生成列参与唯一索引。并发失败方重新读取获胜记录，上传链路清理自己刚写入的对象并返回已存在结果。结果是应用层提供快速路径，数据库保证最终唯一性，不同用户仍保持数据和权限隔离。

**追问 1：为什么唯一约束还要考虑软删除？**

**回答：** 场景上，文件表采用逻辑删除后，旧记录仍留在数据库；用户删除视频后应该允许再次上传相同内容。根因是普通的用户加 MD5 唯一索引会让已删除行继续占用键，而把删除标记直接加入唯一键又允许同一用户出现多条未删除记录，只要删除标记值设计不慎就会破坏语义。行动上，我增加生成列：活动行映射为真实 MD5，删除行映射为空值，再对用户和生成列建立唯一约束，利用 MySQL 唯一索引允许多个空值的特性。结果是任意时刻每个用户最多只有一个相同 MD5 的活动文件，同时历史软删除记录不阻塞重传；迁移前还需检查并清理既有重复数据。

**追问 2：MD5 有碰撞风险，为什么还使用它？**

**回答：** 场景上，这里的 MD5 主要承担上传完整性校验和工程去重键，并不是密码签名或对抗恶意碰撞的安全凭证。根因是 MD5 计算快、客户端和服务端支持广，但理论上存在碰撞，恶意用户也不应仅凭摘要获得他人资源。行动上，我把去重范围限制在同一用户，仍执行鉴权、文件类型与实际内容处理，服务端在合并后独立重算摘要，不信任客户端声明；若业务进入高对抗或合规场景，我会升级为 SHA-256，或以文件大小、强摘要组合确认。结果是在当前内容去重目标下取得性能与实现复杂度平衡，同时不会把 MD5 当作授权依据或不可伪造证明。

#### Q6：文件写入 MinIO、元数据写入 MySQL，如何保证一致性？

**回答：** 场景上，完成上传需要先得到完整对象再保存文件记录，但对象存储和数据库无法参加同一个本地事务。根因是任一侧成功后另一侧失败，都可能产生孤儿对象或悬空元数据；把耗时合并和网络上传包在数据库事务里还会拉长锁持有时间。行动上，我让数据库记录只在对象上传成功后创建，并在并发唯一冲突时主动删除失败方对象，读取时也以记录与对象共同有效为准。结果是正常路径和并发去重路径可以收敛，但一般网络故障下仍有补偿边界。我的演进方案是缩短数据库事务，先登记带状态的上传意图，使用唯一对象键和幂等写，提交后异步确认；另由定时对账扫描未确认记录和孤儿对象并安全清理。

**追问 1：为什么不能简单地在数据库回滚时删除 MinIO 对象？**

**回答：** 场景上，事务异常捕获后立即删除对象看似直接，但补偿本身也可能失败，且并发请求可能已经复用了同一个确定对象键。根因是远程删除不受数据库回滚保护，网络超时会产生“不知道删没删”的不确定状态；如果对象键共享，错误补偿还可能删除获胜事务正在引用的对象。行动上，我会为每次上传使用可归属的临时对象键，数据库提交后再原子发布或更新引用，并把补偿任务持久化，删除前校验对象归属、引用计数或当前记录状态。结果是失败补偿变得可重试、可审计，不依赖一次异常处理成功；现有实现只覆盖明确的并发输家清理，因此我会如实将全面对账列为后续工作。

**追问 2：上传合并为什么不适合放在长数据库事务中？**

**回答：** 场景上，合并大量分片、计算整文件摘要和上传对象都可能持续较久，而且耗时受磁盘和网络波动影响。根因是数据库事务持续期间会占用连接、保存事务上下文，相关行锁和版本也可能阻塞其他请求，吞吐下降后还会形成连接池级联拥塞。行动上，我会将流程改成短事务状态机：先用条件更新把会话从上传中切到合并中，事务外完成合并和对象写入，再用短事务登记成功；失败则记录可重试状态，由清理任务处理临时产物。结果是数据库只负责状态裁决，不承担远程 I/O 生命周期；重复完成请求通过状态和稳定对象键复用同一结果。

### 主题三：多模态解析与时间轴

#### Q7：长音频 ASR 为什么要切片，怎样处理切片边界重复和时间戳？

**回答：** 场景上，长视频音频可能超过供应商单任务的大小或时长限制，直接提交还会让一次失败的重试成本过高。根因是简单无重叠切片容易把跨边界词句截断，而直接拼接带重叠结果又会产生重复文本和错误时间。行动上，我规划固定逻辑窗口，提取音频时在两侧增加小幅物理重叠，并统一转成 16kHz 单声道 PCM 以控制大小；识别结果先加上窗口起点恢复绝对时间，再只保留“片段中点落入该逻辑窗口”的结果。结果是边界处能保留足够语音上下文，又能用确定规则消除大部分重复，各片失败也可独立重试，最终时间轴仍保持单调有序。

**追问 1：为什么用片段中点归属，而不是按文本内容去重？**

**回答：** 场景上，相邻切片的重叠区域可能返回标点、分词和文字略有不同的识别结果，内容相等判断并不稳定。根因是编辑距离或语义相似度需要阈值，短句、重复口头语和数字容易误删，而且算法成本随片段增加。行动上，我利用每个切片已知的逻辑时间边界，把识别片段的中点作为唯一归属依据：只有中点落在当前逻辑窗口才保留，边界情况再约定左闭右开。结果是判定只依赖时间、复杂度低且可复现，不受供应商文本格式影响；它不能修复供应商自身的时间戳漂移，因此我还会用人工标注语料测边界漏词率和重复率。

**追问 2：外部 ASR 创建任务成功后，服务进程还没保存任务 ID 就宕机怎么办？**

**回答：** 场景上，这是调用非事务外部 API 的典型不确定窗口：供应商可能已经受理，但本地仍停留在“提交中”。根因是远程创建和本地持久化无法原子完成，如果马上重提会产生重复供应商任务，如果永不重提又可能永久卡住。行动上，我在提交前持久化分片状态，成功后立即保存供应商任务 ID；恢复时优先轮询已有 ID，对提交中但无 ID 的记录设置不确定等待期，超过供应商任务生存窗口后才允许重新提交，并让最终结果按分片身份幂等覆盖。结果是常见重启能继续轮询，最危险窗口被限制但无法彻底消除；若供应商支持客户端幂等键，我会将其作为根治方案。

#### Q8：为什么 ASR 和 OCR 要并行，失败策略如何设计？

**回答：** 场景上，语音转写和关键帧识别都依赖外部或重计算能力，彼此输入独立，但最终都服务于时间轴。根因是串行执行会把两条耗时直接相加，而“一路失败全部失败”又会因辅助视觉信息不可用而丢掉仍有价值的语音内容。行动上，我在有界执行资源中并行启动两路，统一等待并收集结果；ASR 被定义为主内容来源，空结果或失败会使该阶段进入可重试失败；OCR 是增强来源，异常会记录并降级为空视觉结果，后续仍可用语音构建时间轴。结果是正常情况下缩短总关键路径，OCR 故障时仍能提供摘要和问答基础；同时通过状态和日志暴露降级，避免把质量下降伪装成完全成功。

**追问 1：为什么 OCR 可以降级，ASR 不可以？**

**回答：** 场景上，我需要根据业务价值定义失败语义，而不是机械地要求每个子任务都成功。根因是多数视频的叙事主体来自语音，OCR 主要补充幻灯片标题、代码和画面文字；若没有 ASR，剩余零散关键帧往往无法支持连贯总结，而没有 OCR 仍可提供基本内容理解。行动上，我把 ASR 设为必要证据，把 OCR 设为可选增强，分别配置异常传播和检查点状态；对无语音但以字幕为主的视频，未来可通过内容类型检测切换策略。结果是当前主流语音视频获得更高可用性，故障影响被限定；我也会统计 OCR 降级率和问答质量差异，验证这一产品假设是否长期成立。

**追问 2：并行任务如何避免线程池被长任务拖垮？**

**回答：** 场景上，ASR 和 OCR 都可能等待远程响应，如果每个视频无限创建线程，大量并发会快速耗尽 CPU、内存和连接。根因是默认公共线程池缺乏业务隔离，阻塞调用还会让队列增长，最终连普通接口都受影响。行动上，我使用有界执行器，限制核心线程、队列容量和每次工作流的截止时间；对外部调用设置连接、读取和轮询超时，提交被拒绝时让任务进入明确的可重试状态，而不是无界排队。结果是系统以受控失败换取整体稳定性，单个慢任务不会无限占用资源；容量上限仍需根据真实视频时长分布和外部服务配额压测后配置。

#### Q9：你怎样把语音和关键帧文字融合成可检索的时间轴？

**回答：** 场景上，ASR 返回带起止时间的语音片段，OCR 返回离散帧上的文字和置信度，两路格式、密度与噪声完全不同。根因是直接拼接会打乱时间顺序，同一幻灯片连续抽帧会重复，而过细语音片段也会破坏检索上下文。行动上，我先清洗、排序并规范时间；语音按字符上限和静默间隔合并成语义块，视觉文本先过滤低置信度结果，再依据相邻文本相似度合并为时间跨度；最后按起始时间交错生成带时间标记的 Markdown 时间轴。结果是检索既能命中“说了什么”，也能命中“画面展示什么”，回答可引用时间范围；当前产物是分层时间轴文本，而不是一个神秘的通用多模态对象。

**追问 1：OCR 关键帧如何抽取，为什么不能固定每秒一帧？**

**回答：** 场景上，固定时间抽帧会在静态演示文稿中产生大量重复 OCR，同时仍可能错过采样点之间短暂出现的内容。根因是时间间隔与真实画面变化没有稳定对应关系，当前样本中二十余次视觉观察最终只压缩为一个有效区间，额外计算没有形成新证据。行动上，我改为由场景变化检测选择候选帧，取消固定间隔补帧；当候选数量超过预算时，不直接截断视频后半段，而是从完整候选序列中均匀保留并覆盖首尾，再通过 OCR 置信度和文本相似度压缩重复结果。结果是提帧行为与内容变化直接关联，静态画面的 OCR 调用显著减少；场景阈值仍需用文字召回率、候选帧数量和 OCR 耗时共同评估。

**追问 2：时间轴融合怎样避免同一页 PPT 的文字反复出现？**

**回答：** 场景上，一页幻灯片可能在多个连续关键帧中出现，OCR 结果还会因清晰度不同产生少量字符差异。根因是仅用完全相等去重会保留近似重复，仅按时间合并又可能把真正切页后的相似标题误合并。行动上，我先做空白与噪声清洗，过滤低置信度文本，再计算相邻视觉文本相似度；达到阈值时延长现有视觉区间并保留较完整文本，低于阈值则开启新区间，同时利用帧时间维持局部连续性。结果是同页内容被压缩为一个有起止时间的视觉证据，显著减少索引重复；对于模板高度相似的页面，仍需结合版面或图像特征进一步区分。

### 主题四：RAG 与 Elasticsearch

#### Q10：视频和文档进入知识库后，你如何切块和建立索引？

**回答：** 场景上，整段视频时间轴或整篇文档直接做一个向量会丢失局部主题，切得过碎又会让回答缺少上下文。根因是检索粒度需要在召回精度、语义完整性、模型上下文成本之间平衡，而且视频还必须保留时间定位。行动上，我按段落和长度边界做语义切块，必要时保留相邻重叠，并为每块保存用户、知识库、来源、版本、序号和时间范围等元数据；文本用于关键词字段，嵌入用于稠密向量字段。重建时先写入新版本，完成后再发布可见版本。结果是查询能同时做全文和向量检索，并通过元数据过滤实现隔离与引用定位；发布前版本不可见，避免读到半成品索引。

**追问 1：切块大小和重叠窗口怎么确定？**

**回答：** 场景上，没有一个切块参数适合所有视频、会议记录和普通文档。根因是块太大时一个向量混合多个主题，相关片段被平均；块太小时关键词和指代上下文断裂，召回数量和精排成本也上升。行动上，我先根据模型上下文限制设字符或令牌上限，优先在段落、句子和时间段边界切分，并用小范围重叠保留跨边界语义；随后建立人工标注问题集，对不同大小测 Recall@K、MRR、引用完整性、候选数量和延迟。结果是参数由质量与成本共同决定，而不是写死后宣称最优；目前实现提供确定切块规则，最佳参数仍标记为待测。

**追问 2：为什么索引要版本化发布？**

**回答：** 场景上，视频重新解析、嵌入模型升级或切块策略调整时，需要重建一整批文档，重建期间用户仍可能查询。根因是原地逐条覆盖会让一次请求同时命中新旧块，或者在中途只看到部分内容，产生重复引用和不完整回答。行动上，我为一轮物化生成稳定版本，把全部新块写入并验证数量与元数据，完成后再切换已发布版本过滤条件，最后异步清理旧版本。结果是查询始终只看到一个完整版本，重建失败不会破坏当前线上数据；代价是短期占用双份存储，因此需要监控旧版本滞留并设计清理重试。

#### Q11：为什么选择 BM25、向量检索、RRF 和 Rerank 这四层组合？

**回答：** 场景上，用户问题既包含专有名词、编号和原句，也包含同义改写和概念性描述。根因是 BM25 擅长精确词项但语义泛化弱，向量召回理解语义却可能忽略数字和罕见实体；两路原始分数的量纲和分布不同，直接加权不稳定。行动上，我并行获取关键词和向量候选，先按文档身份去重，再使用倒数排名融合，只依赖各路线名次而不比较原始分数；随后把较小候选集交给交叉编码精排，最终限制上下文条数。结果是精确匹配和语义召回互补，精排成本受到控制；参数和收益需要通过消融实验验证，我不会仅凭架构复杂度声称效果提升。

**追问 1：请你解释 RRF 的原理，为什么它适合混合检索？**

**回答：** 场景上，同一文档可能在关键词路线排第一、在向量路线排第五，而另一文档只在某一路排名靠前。根因是不同检索器分数不可校准，但排名位置具有相对稳定的含义。行动上，我对每一路第 r 名贡献一个 `1/(k+r)` 形式的分值，同一文档跨路线贡献相加，再按融合分排序；常数 k 用来平滑头部差异，并为相同得分设置稳定次序。结果是同时被多路认可的候选自然上升，某一路异常高分不会直接支配结果，新增检索路线也不必重新训练分数标定。局限是它忽略原始分数差距，因此仍需要后续精排判断细粒度相关性。

**追问 2：有了 Rerank，为什么还需要前面的混合召回？**

**回答：** 场景上，精排模型通常能更准确判断查询与文本块关系，但它只能重排已经进入候选集的内容。根因是如果召回阶段漏掉正确证据，精排无法凭空找回；让精排扫描全库又会产生不可接受的延迟和算力成本。行动上，我让 BM25 与向量检索以较低成本扩大互补候选，用排名融合压缩并去重，再把有限数量交给精排，最终只把最相关的少量证据放入大模型上下文。结果是召回负责“别漏”，精排负责“别乱”，生成阶段上下文更集中；我会分别测召回率、精排后的 MRR 和端到端引用准确率，判断每层是否值得保留。

#### Q12：检索服务如何做故障降级和数据隔离？

**回答：** 场景上，向量生成、Elasticsearch 关键词查询和外部精排都可能独立超时，但知识问答不能因为非核心增强能力失效就完全不可用。根因是串行强依赖会放大单点故障，同时多用户知识库若只在应用层过滤会有越权风险。行动上，我把用户标识、知识库范围和已发布状态作为每条检索路线的强制过滤条件；BM25 或向量单路失败时使用另一条路线，精排失败时保留融合顺序，两条召回都失败才返回明确不可用。结果是系统能以质量下降换取可用性，且降级不绕过权限过滤；每次降级都应记录原因、耗时和候选数，便于区分依赖故障与正常低召回。

**追问 1：如果向量服务故障，只用 BM25 会有什么影响？**

**回答：** 场景上，向量生成或 kNN 查询不可用时，用户仍可能提出包含原文关键词的问题。根因是 BM25 不依赖嵌入服务，能维持实体词和原句匹配，但对同义表达、概括性问题和跨措辞关联的召回明显变弱。行动上，我捕获向量路线的可分类异常，继续执行或保留 BM25 候选，跳过需要双路的融合并在精排可用时继续精排，同时在响应和监控事件中标记降级。结果是系统不会因增强路线失败直接中断，但回答置信度可能下降；我会用评测集比较正常与降级模式的 Recall@K 和无答案率，决定是否提示用户重试。

**追问 2：仅依赖 userId 过滤就能确保租户隔离吗？**

**回答：** 场景上，用户一次对话还会绑定特定知识库范围，视频删除或索引重建后也不能命中过期版本。根因是只有用户过滤虽然阻止跨用户访问，却可能在同一用户的不同知识库之间串数据；若客户端可自行提交任意用户标识，还会形成直接越权。行动上，我从认证上下文取得用户身份，不信任请求体中的身份字段，并在服务端解析当前会话固定的知识库集合；所有关键词和向量查询同时加入用户、知识库、发布状态等过滤，生成引用前再次校验证据范围。结果是隔离约束贯穿两路检索与对话生命周期，而不是查询结束后再删结果；删除流程还会异步清除对应索引和缓存。

### 主题五：智能体问答与工程质量

#### Q13：Planner–Executor–Critic 工作流如何减少无关检索和错误引用？

**回答：** 场景上，单次固定检索可能遗漏证据，而完全自治的智能体又可能反复调用工具、改写偏题甚至引用不存在的材料。根因是大模型擅长规划但输出不稳定，必须把自主性限制在后端可验证协议内。行动上，我让规划阶段只能从白名单检索工具中生成结构化计划，执行阶段受总调用次数、去重规则和截止时间约束，审查阶段只允许选择真实候选标识并判断证据是否充分；不足时最多进行有限重规划和定向改写。结果是补证由明确缺口触发，成本和循环次数有上界，最终引用来自实际候选。当前规划器不可用时会明确失败，尚未实现规则式规划兜底，我不会把它描述成已有能力。

**追问 1：Critic 会不会也产生幻觉，你怎么约束？**

**回答：** 场景上，审查器本身仍是大模型，如果允许它自由编写引用，它可能虚构候选或误判证据。根因是自然语言输出缺乏引用完整性约束，模型不知道哪些标识真实存在。行动上，我只向它提供有限候选及标识，要求返回结构化的接受、补证或越界结论，并在后端验证所选标识必须属于当前候选集合、数量不得超限；另外用确定性规则检查证据是否为空、时间范围是否合法、同一证据是否冲突。结果是模型负责相关性判断，程序负责集合边界和结构正确性；非法输出不会直接进入回答，而是触发受限重试或明确失败。

**追问 2：查询改写怎样避免把用户原意改坏？**

**回答：** 场景上，补证时改写能扩展同义表达，但编号、引号内容、数字和大写缩写一旦丢失，检索会偏离原问题。根因是生成式改写倾向于概括和润色，可能把高信息量实体替换成泛化词。行动上，我从原查询提取受保护词项，校验改写必须保留引号短语、标识符、数字和关键大写词，并限制改写数量；检索结果与原始问题已获得的证据合并，而不是用改写结果覆盖原证据。结果是改写只作为定向补充，重复或越界改写会被拒绝。需要说明的是，当前没有独立的“改写失败后重新跑一次原查询”分支，原始证据保留才是现有回退基础。

#### Q14：多轮对话上下文和流式问答怎样保证正确性与资源安全？

**回答：** 场景上，多轮问答需要快速读取历史和固定知识库范围，流式生成又可能因客户端断开、超时或高并发持续占用后台资源。根因是只依赖 Redis 会在缓存故障时丢上下文，只依赖数据库则增加每轮延迟；异步工作若不感知取消，会继续检索和生成无用结果。行动上，我采用缓存旁路：优先读取热快照并校验知识库范围指纹，未命中或异常时回源 MySQL，再尝试回填；工作流在有界线程池中运行，设置截止时间并周期检查取消信号，SSE 断开时向下传播取消。结果是缓存提升性能但不成为正确性来源，对话范围不会被后续请求悄悄扩大，断开的请求也能尽早释放资源。

**追问 1：Redis 故障时为什么还能继续问答？**

**回答：** 场景上，Redis 在这里只保存可重建的热对话快照，网络故障或重启不应该破坏持久事实。根因是如果把唯一上下文只放缓存，缓存就升级成关键数据库，恢复和一致性成本都会增加。行动上，我把会话、消息和固定知识库范围保存在 MySQL，读取缓存时验证会话版本和范围指纹；缓存未命中、反序列化失败或连接异常时直接回源数据库，成功后以尽力而为方式重建快照，回填失败不影响当前答案。结果是 Redis 故障主要增加延迟而不改变权限和内容正确性；我通过停 Redis 的端到端场景验证回源路径，并计划量化故障时 P95。

**追问 2：SSE 客户端断开后，服务端任务一定会立刻停止吗？**

**回答：** 场景上，浏览器关闭页面后，网络层能感知连接结束，但正在等待的外部模型或检索调用未必支持立即中断。根因是取消在 Java 线程、异步任务和远程 HTTP 客户端之间不是天然传递，某些阻塞调用只会在自身超时后返回。行动上，我在工作流关键循环检查取消标记，为线程任务设置可中断状态，为外部调用配置较短的连接与读取超时，并在结果回写前再次确认请求仍有效；无法硬取消的调用依靠截止时间释放。结果是大部分后续步骤能停止，资源占用有最大上界，但我不会承诺毫秒级立即终止；还需用连接中断压测观测残留任务数。

#### Q15：你如何测试这套系统？目前最重要的工程债务是什么？

**回答：** 场景上，这个系统跨 MySQL、Redis、RocketMQ、MinIO、Elasticsearch、FFmpeg 和外部 AI 服务，只测成功接口不足以证明可靠性。根因是多数缺陷出现在重复投递、依赖重启、部分成功和超时边界。行动上，我建立单元、组件和真实基础设施端到端分层测试，覆盖消息重投、Broker 重启、Redis 故障回源、长音频、删除清理与 RAG；最近一次后端回归为 247 个测试通过、0 失败、7 个真实环境测试因依赖未启动而跳过。结果是功能回归有基线。当前最高优先债务是长任务租约未周期续期，其次是上传状态重建、缩短跨存储长事务和完善孤儿对象对账。

**追问 1：怎样验证断点续跑真的没有重复副作用？**

**回答：** 场景上，普通重跑成功只能证明最终可用，不能证明中间没有重复调用 ASR、重复写索引或重复生成摘要。根因是故障注入点不同，会落在“产物完成但检查点未写”或“检查点写完但消息未确认”等窗口。行动上，我会在每个阶段前后设置可控故障点，记录稳定任务、阶段、外部请求和文档标识；强杀进程后重投同一事件，核对调用次数、检查点校验和、索引版本和最终记录数。结果是可以区分安全重放与真实重复副作用，并计算检查点复用率和恢复时间。对无法提供幂等键的外部服务，我会把重复调用风险单独列出而不是用最终一致掩盖。

**追问 2：如果让你再做一轮优化，你会先做什么？**

**回答：** 场景上，系统已有完整功能链，但长视频处理时间可能超过固定租期，这是会影响正确性而非只影响性能的问题。根因是状态机虽然具备续期接口，执行主链尚未定时调用；租约过期后重复投递可能让第二实例接管，两个实例并行产生昂贵副作用。行动上，我会先实现带所有权令牌的周期续约，续约失败立即停止后续写入，并让每个阶段提交前再次校验持有权；随后用超过租期的视频、消费者停顿和重复投递做故障测试。结果目标是同一时刻只有有效持有者推进，旧实例无法覆盖新状态。完成正确性修复后，我才会继续做上传事务缩短和检索参数压测。

## 4. 源码证据索引

> 本节用于复习和现场被追问时定位实现。口述时优先使用“事务消息、消费 Inbox、租约状态机、阶段检查点”等通用工程语言，不要背诵类名。

| 主题 | 仓库相对路径与内部符号 | 对应问题 |
|---|---|---|
| 总体链路 | `README.md`；上传→事务消息→任务消费→解析→索引→问答 | Q1 |
| 事务消息生产 | `backend/videomind-server/src/main/java/com/videomind/module/task/mq/RocketMqTransactionalTaskMessageProducer.java`；`sendInTransaction` | Q2 |
| 本地事务与回查 | `backend/videomind-server/src/main/java/com/videomind/module/task/mq/TaskTransactionListener.java`；`executeLocalTransaction`、`checkLocalTransaction` | Q2 |
| 任务幂等创建 | `backend/videomind-server/src/main/java/com/videomind/module/task/service/impl/LocalTaskTransactionServiceImpl.java`；业务指纹与事件落库 | Q2、Q3 |
| 消费入口 | `backend/videomind-server/src/main/java/com/videomind/module/task/service/impl/TaskEventConsumerServiceImpl.java`；Inbox claim、CAS acquire、状态投影 | Q3 |
| 消费 Inbox | `backend/videomind-server/src/main/java/com/videomind/module/task/service/impl/ConsumerInboxServiceImpl.java`；`claim`、`complete` | Q3 |
| 任务状态机 | `backend/videomind-server/src/main/java/com/videomind/module/task/service/impl/ProcessingTaskStateMachineImpl.java`；`acquire`、`renew`、`markRetryWait` | Q3、Q15 |
| 阶段检查点 | `backend/videomind-server/src/main/java/com/videomind/module/task/service/impl/TaskCheckpointServiceImpl.java`；阶段唯一键、checksum 冲突检查 | Q3、Q15 |
| 分片上传 | `backend/videomind-server/src/main/java/com/videomind/module/video/service/impl/MultipartUploadServiceImpl.java`；分片锁、Bitmap、merge、整文件 MD5 | Q4、Q5、Q6 |
| 活动文件唯一性 | `backend/videomind-server/src/main/resources/db/migration/V21__unique_video_file_user_md5.sql`；`active_file_md5`、`uk_video_file_user_md5` | Q5 |
| 并发插入 | `backend/videomind-server/src/main/java/com/videomind/module/video/mapper/VideoFileMapper.java`；`insertIgnore` | Q5、Q6 |
| 解析编排 | `backend/videomind-server/src/main/java/com/videomind/module/task/analysis/VideoAnalysisHandler.java`；六个阶段检查点与恢复 | Q1、Q3、Q8 |
| 并行 ASR/OCR | `backend/videomind-server/src/main/java/com/videomind/module/task/analysis/ParallelVideoAnalysisStage.java`；`CompletableFuture` 与 OCR 降级 | Q8 |
| 音频窗口 | `backend/videomind-server/src/main/java/com/videomind/module/task/analysis/chunk/AudioChunkPlanner.java`；逻辑窗口与重叠 | Q7 |
| 音频切片 | `backend/videomind-server/src/main/java/com/videomind/module/task/analysis/chunk/FfmpegAudioChunker.java`；16kHz、单声道、PCM | Q7 |
| 外部 ASR 恢复 | `backend/videomind-server/src/main/java/com/videomind/module/task/analysis/tencent/TencentAsrChunkTranscriber.java`；提交状态、TaskId 持久化、轮询 | Q7 |
| ASR 合并 | `backend/videomind-server/src/main/java/com/videomind/module/task/analysis/chunk/AsrChunkResultMerger.java`；绝对时间与中点归属 | Q7 |
| 关键帧抽取 | `backend/videomind-server/src/main/java/com/videomind/module/task/analysis/ocr/FfmpegKeyframeExtractor.java`；场景阈值与候选帧预算 | Q9 |
| 时间轴融合 | `backend/videomind-server/src/main/java/com/videomind/module/knowledge/timeline/TimelineFusionService.java`；speech block、visual span | Q9 |
| 时间轴物化 | `backend/videomind-server/src/main/java/com/videomind/module/knowledge/timeline/VideoTimelineMaterializer.java`；`timeline.md` 与元数据 | Q9、Q10 |
| 语义切块与索引 | `backend/videomind-server/src/main/java/com/videomind/module/knowledge/chunk/SemanticChunker.java`；`backend/videomind-server/src/main/java/com/videomind/module/knowledge/timeline/TimelineKnowledgeIndexer.java` | Q10 |
| ES 映射与查询 | `backend/videomind-server/src/main/java/com/videomind/module/knowledge/retrieval/ElasticsearchGateway.java`；IK、dense_vector、过滤 | Q10、Q12 |
| 混合检索 | `backend/videomind-server/src/main/java/com/videomind/module/knowledge/retrieval/HybridRetrievalService.java`；双路召回、降级、精排 | Q11、Q12 |
| RRF | `backend/videomind-server/src/main/java/com/videomind/module/knowledge/retrieval/VideoMindReciprocalRankFuser.java`；排名去重与稳定排序 | Q11 |
| 精排 | `backend/videomind-server/src/main/java/com/videomind/module/knowledge/retrieval/RealRerankClient.java`；BGE 响应校验 | Q11、Q12 |
| PEC 工作流 | `backend/videomind-server/src/main/java/com/videomind/module/agent/workflow/PlannerExecutorCriticWorkflow.java`；预算、去重、重规划、截止时间 | Q13、Q14 |
| 结构化规划 | `backend/videomind-server/src/main/java/com/videomind/module/agent/workflow/StructuredLlmAgentPlanner.java`；工具白名单与 JSON 校验 | Q13 |
| 证据审查 | `backend/videomind-server/src/main/java/com/videomind/module/agent/workflow/StructuredLlmAgentCritic.java`；候选 ID 校验 | Q13 |
| 确定性审查 | `backend/videomind-server/src/main/java/com/videomind/module/agent/workflow/EvidenceAgentCritic.java`；证据完整性和冲突 | Q13 |
| 查询改写保护 | `backend/videomind-server/src/main/java/com/videomind/module/agent/workflow/QueryRewriteGuard.java`；quoted term、identifier、number | Q13 |
| 有界执行与取消 | `backend/videomind-server/src/main/java/com/videomind/module/agent/workflow/WorkflowDecisionRunner.java`；固定线程、有限队列、取消检查 | Q14 |
| 对话上下文 | `backend/videomind-server/src/main/java/com/videomind/module/chat/service/impl/ConversationContextServiceImpl.java`；范围指纹、MySQL 回源 | Q12、Q14 |
| Redis 热快照 | `backend/videomind-server/src/main/java/com/videomind/module/chat/service/impl/RedisHotConversationSnapshotService.java`；缓存读取与回填 | Q14 |
| 端到端故障验证 | `ops/e2e-local.ps1`；ASR/OCR、RAG、Broker 重启、Redis 故障、删除与长音频场景 | Q15 |

## 5. 量化与验证（含待测）

面试中只引用已经有证据的数字：最近一次后端自动化回归为 **247 个测试通过、0 失败、0 错误，7 个依赖真实基础设施的集成测试跳过**。这不代表生产吞吐或检索效果。下面的指标在取得真实结果前统一回答“待测”，并说明测试方案。

| 面试主张 | 指标 | 如何测 | 状态 |
|---|---|---|---|
| 分片上传提升弱网可靠性 | 续传成功率、重传字节比例、完成耗时 P95 | 注入延迟、丢包和连接中断，对比整文件重传 | 待测 |
| 并发去重正确 | 同用户同 MD5 活动记录数、孤儿对象数 | 并发完成相同文件，联合核对 MySQL 与 MinIO | 数据库约束和自动化测试已完成；真实对象存储并发待测 |
| 检查点降低重试成本 | 阶段复用率、重复外部调用数、恢复时间 | 在每个阶段提交前后强杀消费者并重投 | 待测 |
| 并行解析缩短耗时 | 串行与并行总耗时 P50/P95、资源占用 | 使用相同视频重复运行并隔离外部服务抖动 | 待测 |
| 混合检索提升质量 | Recall@K、MRR、nDCG、引用准确率 | 建立人工标注集，对 BM25、向量、RRF、Rerank 做消融 | 待测 |
| 检索降级保持可用 | 故障成功率、质量下降、P95 | 分别关闭向量、关键词、精排依赖并回放问题集 | 逻辑已实现；量化待测 |
| PEC 减少错误引用 | 无依据引用率、首轮接受率、平均工具调用数、补证成功率 | 与单次检索基线做同集对照，保存决策事件 | 待测 |
| Redis 故障可回源 | 故障请求成功率、回源 P95、恢复后命中率 | 停止 Redis 后连续问答，再恢复并观察回填 | E2E 路径存在；量化待测 |
| 长任务租约安全 | 重复执行数、失效持有者写入数 | 构造超出租期任务并重复投递，接入续约前后对比 | 当前缺口，待实现后验证 |

推荐口径：**“功能正确性已经有自动化回归证据；性能、稳定性和检索效果只有完成上述压测与标注评测后才会写成百分比或倍数。”**
